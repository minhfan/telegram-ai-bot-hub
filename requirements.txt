# Lò Rèn Chronos — Desktop App
# Chạy app (Mac/Win/Linux) chỉ cần CustomTkinter; HTTP + GUI còn lại dùng stdlib (urllib, tkinter).
customtkinter>=5.2.2

# Chỉ cần khi ĐÓNG GÓI .exe trên Windows (GitHub Actions tự cài, không bắt buộc local):
# pyinstaller>=6.0
